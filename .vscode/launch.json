{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/cinemasphere-back/package.json",
                "localRoot": "${workspaceFolder}/cinemasphere-back"
            }
        }
    ]
}